load("@rules_tectonic//tectonic:defs.bzl", "tectonic_pdf")

package(default_visibility = ["//visibility:public"])

# bazel build //bus_mods/jesssullivan/port_side_shelves/docs:cut_list
# -> bazel-bin/bus_mods/jesssullivan/port_side_shelves/docs/cut_list.pdf
#
# ply_patterns.tex is generated: ./ply_patterns.py (reads ply_outlines.json,
# which is harvested from the Fusion model over the Fusion MCP).
tectonic_pdf(
    name = "cut_list",
    src = "cut_list.tex",
    reruns = 1,
    deps = ["ply_patterns.tex"],
)
